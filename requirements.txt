# Core dependencies for doc-scan-intelligent-operator
# Minimum versions for compatibility - managed by Renovate

# MLX for Apple Silicon acceleration
mlx>=0.26.0
mlx-lm>=0.23.0
mlx-vlm>=0.3.0

# PyTorch (CPU-only for processor compatibility, MLX handles actual inference)
torch>=2.0.0
torchvision>=0.15.0

# Hugging Face ecosystem (pulled by mlx-vlm but specify for clarity)
huggingface-hub>=0.25.0
transformers>=4.57.0
tokenizers>=0.22.0

# Configuration
PyYAML>=6.0

# PDF processing
PyMuPDF>=1.26.0  # Also known as fitz, for PDF to image conversion (updated for security fix)
Pillow>=10.0.0  # Image processing

# OCR for text extraction
pytesseract>=0.3.10
