name: SonarCloud Analysis

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for better analysis

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run tests with coverage
        run: |
          pytest tests/ --cov=docscan --cov-report=xml:coverage.xml --cov-report=term
        continue-on-error: true

      - name: Fix coverage paths for SonarCloud
        run: |
          # Convert absolute paths to relative paths in coverage.xml
          sed -i 's|<source>/home/runner/work/doc-scan-intelligent-operator/doc-scan-intelligent-operator</source>|<source>.</source>|g' coverage.xml
          sed -i 's|filename="/home/runner/work/doc-scan-intelligent-operator/doc-scan-intelligent-operator/|filename="|g' coverage.xml

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v3.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
